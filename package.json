{
  "name": "rook",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm ci && npx tsc && cd ../frontend && npm ci && npm run build",
    "start": "cd backend && node dist/index.js"
  }
}