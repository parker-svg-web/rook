services:
  - type: web
    name: rook
    runtime: node
    buildCommand: cd backend && npm install && npx tsc && cd ../frontend && npm install && npm run build
    startCommand: node backend/dist/index.js
    healthCheckPath: /api/health
    env:
      - key: PORT
        value: "4000"